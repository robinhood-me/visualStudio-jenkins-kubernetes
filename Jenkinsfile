pipeline {
    agent any
    stages {
        stage('test') {
            steps {
                bash '''#!/bin/bash"
                    echo 'running tests'
            }
        }
        stage('build') {
            steps {
                bash '''#!/bin/bash"
                    echo 'building App'
            }
        }
        stage('deploy') {
            steps {
                bash '''#!/bin/bash"
                    echo 'Hello World'
            }
        }
    }
   }