stages {
      stage('test') {
         steps {
            echo 'running tests'
         }
      }
       stage('build') {
         steps {
            echo 'building App'
         }
      }
       stage('deploy') {
         steps {
            echo 'Hello World'
         }
      }
   }